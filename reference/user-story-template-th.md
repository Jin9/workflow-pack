<!--
  =========================================================================
  USER-STORY TEMPLATE (เทมเพลตสำหรับเขียน User Story)
  =========================================================================
  วิธีใช้งาน
  1. คัดลอกไฟล์นี้และเปลี่ยนชื่อตาม story ของคุณ (เช่น ISSUE-KEY-slug.md)
  2. แทนที่ `<placeholder>` ทุกจุด (ที่อยู่ในเครื่องหมาย backticks และวงเล็บแหลม) ด้วยข้อมูลจริง
  3. คำแนะนำจะอยู่ใน HTML comments ที่ขึ้นต้นด้วย HOW: และ EXAMPLE: ซึ่งจะไม่แสดง
     ตอน render ไฟล์ Markdown; สามารถลบออกก่อนเผยแพร่ได้หากต้องการความสะอาดของไฟล์
  4. เก็บไว้เฉพาะหัวข้อที่ต้องใช้และลบส่วนที่เหลือทิ้ง หัวข้อที่บังคับมีเพียง Title และ Description เท่านั้น
  5. หัวข้อที่มีแท็ก METADATA มักจะถูกใส่ข้อมูลอัตโนมัติโดยระบบ Tracker (เช่น Jira) — ให้เก็บไว้
     เฉพาะเวลาที่ต้องการคัดลอกข้อมูลทั้งหมดจากตั๋ว (ticket export)
  เนื้อหาใน Story สามารถเขียนด้วยภาษาอะไรก็ได้; แต่โครงสร้างนี้ใช้ภาษาไทย
  =========================================================================
-->

`< กลับ`  ·  `<ISSUE-KEY>`  ·  `<RELATED-KEY>`

<!-- HOW: Breadcrumb และรหัส issue (issue key) ตามที่ปรากฏใน tracker ให้ระบุรหัสที่เกี่ยวข้องตามหลังรหัสหลัก หากเครื่องมือของคุณแสดงรหัสเหล่านี้ที่อื่นแล้ว ให้ลบบรรทัดนี้ทิ้ง -->
<!-- EXAMPLE: `< กลับ` · PROJ-4821 · PROJ-4822 -->

# [`<TYPE>`] `<ชื่อเรื่องแบบกระชับ (imperative title)>`

<!-- HOW: ระบุประเภทในวงเล็บก้ามปู (type tag) ตามด้วยชื่อเรื่องสั้นๆ ที่เข้าใจง่าย ประเภทที่ใช้บ่อย: [BAU], [Feature], [Bug], [Spike] -->
<!-- EXAMPLE: # [BAU] เพิ่มข้อเสนออัตราดอกเบี้ยแบบขั้นบันได (tier-rate) สำหรับสินเชื่อ Meridian Flexi Loan -->

## รายละเอียด (Description)

<!-- HOW: รูปแบบคลาสสิกของ user story 3 ส่วน ได้แก่ บทบาท (role), ความสามารถที่ต้องการ (capability), และคุณค่าทางธุรกิจ (business value) โดยในส่วน "ฉันต้องการ" ให้ระบุเพียง 1 ความสามารถเท่านั้น -->
<!-- EXAMPLE: ในฐานะ หน่วยธุรกิจ / ฉันต้องการ เสนออัตราดอกเบี้ยแบบขั้นบันไดในผลิตภัณฑ์สินเชื่อบุคคลสำหรับลูกค้าที่มีรายได้เกินเกณฑ์ที่ระบบประเมินความเสี่ยงกำหนด (30,000 บาท) / เพื่อที่ว่า อัตราที่เสนอจะสอดคล้องกับฐานเงินเดือนและสามารถแข่งขันในตลาดได้ -->

*ในฐานะ (As a)* `<บทบาท / ผู้ใช้งาน (role / persona)>`

*ฉันต้องการ (I want)* `<ความสามารถที่ต้องการ (one capability)>`

*เพื่อที่ว่า (So that)* `<ประโยชน์ / คุณค่าทางธุรกิจ (benefit / business value)>`

## ผลิตภัณฑ์ (Product)

<!-- HOW: ผลิตภัณฑ์หรือฟีเจอร์ที่ story นี้เกี่ยวข้อง (เขียนบรรทัดเดียว) -->
<!-- EXAMPLE: สินเชื่อ Meridian Flexi Loan — สินเชื่อใหม่ -->

`<ผลิตภัณฑ์ / ส่วนของฟีเจอร์ (product / feature area)>`

## ความเป็นมา (Background)

<!-- HOW: เหตุผลที่ต้องมี story นี้ — จุดเริ่มต้น, สถานะก่อนหน้า, หรือตัวผลักดันทางธุรกิจ (business driver) หากมีหลายข้อให้ระบุเป็นตัวเลข -->
<!-- EXAMPLE: 1. เพิ่มข้อเสนออัตราดอกเบี้ยแบบขั้นบันไดสำหรับลูกค้าที่มีรายได้ต่อเดือนเกิน 30,000 บาท -->

1. `<บริบทหรือความเป็นมา (context point)>`

## ตรรกะทางธุรกิจ (Business Logic)

<!-- HOW: กฎที่ระบบต้องบังคับใช้ โดยระบุเป็นข้อๆ ใช้ a./b./c. สำหรับรายละเอียดปลีกย่อย
     เพิ่มตารางหากมีการตัดสินใจแบบมีเงื่อนไข (decision matrices) และใช้ > สำหรับหมายเหตุที่เกี่ยวข้องกับหลายส่วน (เช่น ความยืดหยุ่น, วันที่มีผล, ผู้รับผิดชอบ) -->
<!-- EXAMPLE: 1. รองรับดอกเบี้ยแบบขั้นบันได: (a) requirement ครอบคลุม 2 ขั้น; (b) ระบบต้องรองรับอย่างน้อย 5 ขั้น -->

1. `<กฎเกณฑ์ (rule)>`
   a. `<รายละเอียด (detail)>`
   b. `<รายละเอียด (detail)>`
2. `<กฎเกณฑ์ที่อ้างอิงตารางตัดสินใจด้านล่าง>`

### `<ชื่อตารางการตัดสินใจ (Decision matrix name)>`

<!-- HOW: ตารางตัดสินใจที่เชื่อมโยงเงื่อนไขกับผลลัพธ์ — 1 แถวต่อ 1 กรณี ลบหัวข้อนี้ทิ้งหาก story ไม่มีตารางเงื่อนไข -->
<!-- EXAMPLE: "รายได้ -> ขั้นอัตราดอกเบี้ย": แหล่งที่มาของรายได้ (SA/SE) x การรับเงินเดือนผ่านนายจ้าง (Yes/No) x รายได้ต่อเดือน -> อัตราดอกเบี้ยต่อปี
     เช่น SA + Yes + ">= 30,000 THB (multi-tier)" -> เดือน 1-3: 7.99%, เดือน 4+: 20% -->

| `<มิติที่ 1 (Dimension 1)>` | `<มิติที่ 2 (Dimension 2)>` | `<เงื่อนไข (Condition)>` | `<ผลลัพธ์ (Outcome)>` |
|---|---|---|---|
| `<ค่า>` | `<ค่า>` | `<เงื่อนไข>` | `<ผลลัพธ์>` |
| `<ค่า>` | `<ค่า>` | `<เงื่อนไข (หลายขั้น)>` | `<เช่น เดือน 1–3: อัตราดอกเบี้ย A; เดือน 4+: อัตราดอกเบี้ย B>` |

<!-- HOW: ใช้ > blockquote สำหรับกฎที่ครอบคลุมเงื่อนไขทั้งตารางด้านบน -->
<!-- EXAMPLE: อัตราดอกเบี้ยจะเป็นไปตามที่ระบบประเมินความเสี่ยงส่งค่ากลับมา; ปัจจัยหลักคือ รายได้, แหล่งที่มาของรายได้, การรับเงินเดือนผ่านนายจ้าง, และวันที่มีผล -->
> ⚠️ **หมายเหตุ:** `<กฎหรือข้อยกเว้นที่ครอบคลุมเงื่อนไขด้านบนทั้งหมด>`

## เกณฑ์การยอมรับ (Acceptance Criteria)

<!-- HOW: เงื่อนไขการทดสอบว่าผ่าน/ไม่ผ่าน แต่ละข้อต้องสามารถตรวจสอบได้ และให้ระบุเป็นข้อๆ -->
<!-- EXAMPLE: 1. ระบบเสนอสินเชื่ออัตราเดียวเมื่อรายได้ < 30,000 บาท  2. ระบบเสนออัตราดอกเบี้ยแบบหลายขั้นและสามารถบันทึกได้สูงสุด 5 ขั้น -->

1. `<เงื่อนไขที่สามารถทดสอบได้ (testable condition)>`
2. `<เงื่อนไขที่สามารถทดสอบได้ (testable condition)>`

## นอกขอบเขต (Out of Scope)

<!-- HOW: สิ่งที่ story นี้ไม่ได้ครอบคลุมอย่างชัดเจน เพื่อป้องกัน scope creep; ให้ระบุชื่อทีมรับผิดชอบหากเป็นงานของส่วนอื่น -->
<!-- EXAMPLE: 1. การเปลี่ยนชื่อป้ายแสดง "อัตราดอกเบี้ย" บนหน้าจอแอปมือถือ (รับผิดชอบโดยทีม Mobile App) -->

1. `<สิ่งที่ไม่รวมอยู่ในงานนี้ (excluded item)>`

---

## ข้อมูลอ้างอิงหน้าจอ (Mock-up references)

<!-- HOW: ระบุหมายเลขตามหน้าจอจำลอง (mock-up) แสดงภาพ AS-IS / TO-BE (ก่อน / หลัง) และเพิ่มบรรทัด Spec API หากมีการเปลี่ยนแปลงของ API
     กรณีที่ mock-up เป็นเพียงรูปภาพ ให้อธิบายการเปลี่ยนแปลงที่ไฮไลต์ด้วยข้อความแทนที่จะแปะเพียงรูปภาพ ลบส่วนนี้ทิ้งได้เลยหากไม่มีหน้าจอ -->
<!-- EXAMPLE: 1. การตัดสินใจสินเชื่อ — LOS ถึง DLP (Before/After JSON): โครงสร้าง After (TO-BE) จะเพิ่ม array tierList[]
     (tierStartTier, interestIndex, interestSpread, paymentNo). Spec API: POST /api/rate-tier/updateStatus v1.0.0 -->

### 1. `<ชื่อหน้าจอ (mock-up name)>`

- **AS-IS:** `<สถานะปัจจุบัน (current state)>`
- **TO-BE:** `<สถานะที่เปลี่ยนแปลง — ระบุจุดที่ไฮไลต์การเปลี่ยนแปลง>`
- **Spec API:** `<METHOD /path version>`   <!-- ทางเลือก; ลบทิ้งได้หากไม่มีการเปลี่ยน API -->

---

## ไฟล์แนบ (Attachments) (`<จำนวน>`)

<!-- METADATA — ระบบ tracker มักจะใส่ข้อมูลส่วนนี้ให้อัตโนมัติ ให้เก็บไว้เฉพาะเวลาที่คัดลอกข้อมูลทั้งหมดจากตั๋ว -->
<!-- HOW: รายการไฟล์สนับสนุนพร้อมระบุขนาดและวันที่เพิ่ม -->
<!-- EXAMPLE: | payment_calc.xlsx | 2.2 MB | 15 ส.ค. 2025 | -->

| ชื่อ (Name) | ขนาด (Size) | วันที่เพิ่ม (Date added) |
|---|---|---|
| `<ชื่อไฟล์ (filename)>` | `<ขนาด (size)>` | `<วันที่ (date)>` |

## งานย่อย (Subtasks)

<!-- METADATA — งานย่อย (child tasks) ของ story นี้ -->
<!-- EXAMPLE: - PROJ-XXXX  `<ชื่องานย่อยแบบสั้น>` -->

*เพิ่มงานย่อย (Add subtask)* — `<ไม่มี / รายการ>`

## รายการงานที่เชื่อมโยง (Linked work items)

<!-- METADATA — issue ที่เกี่ยวข้อง (blocks / is blocked by / relates to) -->

*เพิ่มรายการงานที่เชื่อมโยง (Add linked work item)* — `<ไม่มี / รายการ>`

## เนื้อหาใน Confluence (Confluence content)

<!-- METADATA — หน้า Confluence หรือ API spec ที่เชื่อมโยง -->
<!-- EXAMPLE: - POST /api/rate-tier/updateStatus v1.0.0 — Updated -->

- `<หน้าเอกสาร / spec ที่เชื่อมโยง>`

---

## กิจกรรม (Activity)

<!-- METADATA — บันทึกการสนทนา ซึ่งมักจะดึงจาก tracker -->

แท็บ (Tabs): **ทั้งหมด (All) · ความคิดเห็น (Comments) · ประวัติ (History) · บันทึกการทำงาน (Work log) · การอนุมัติ (Approvals)**

### สรุปความคิดเห็น (Comments summary)

<!-- HOW: สรุปสั้นๆ เกี่ยวกับการตัดสินใจที่สำคัญและการอนุมัติ หากมีการจดบันทึกเป็นลายลักษณ์อักษร -->
<!-- EXAMPLE: นำอัตราดอกเบี้ยแบบขั้นบันไดมาใช้เพื่อเพิ่มความสามารถในการแข่งขันสำหรับลูกค้าที่มีรายได้ > 30,000 บาท; เริ่มนำขึ้นระบบช่วงปลายเดือน ส.ค. 2025 โดยรองรับสูงสุด 5 ขั้น -->

`<สรุป (summary)>`
