{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://squad-delivery/schemas/delivery-pipeline-output.json",
  "title": "delivery-pipeline workflow output",
  "description": "Top-level workflow output stub for delivery-pipeline.yaml. Permissive: the per-field shapes live in each stage's own schema; spec.output.required_fields in the YAML enumerates the expected keys. audit_id threads the banking-grade audit trail end to end.",
  "type": "object",
  "required": ["audit_id"],
  "additionalProperties": false,
  "properties": {
    "ba_brief": { "type": "object" },
    "ux_pack": { "type": "object" },
    "tl_design": { "type": "object" },
    "backend_artifacts": { "type": "object" },
    "frontend_artifacts": { "type": "object" },
    "qa_plan": { "type": "object" },
    "qa_evidence": { "type": "object" },
    "handoff_receipt": { "type": "object" },
    "prod_validation": { "type": "object" },
    "audit_id": { "type": "string" }
  }
}
