{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "S-test sast-gate boundary \u2014 SAST findings",
  "type": "object",
  "required": [
    "verdict",
    "findings",
    "secrets",
    "new_vs_baseline",
    "execution",
    "scan_scope",
    "audit_id"
  ],
  "additionalProperties": true,
  "properties": {
    "verdict": {
      "type": "string",
      "enum": [
        "PASS",
        "FAIL",
        "ERROR"
      ]
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "secrets": {
      "type": "integer",
      "minimum": 0
    },
    "new_vs_baseline": {
      "type": "object"
    },
    "audit_id": {
      "type": "string",
      "format": "uuid"
    },
    "human_view": {
      "type": "object",
      "description": "Additive render-policy hint for the human-review layer; absence == default (generic tree).",
      "additionalProperties": true,
      "properties": {
        "kind": {
          "type": "string"
        },
        "render": {
          "type": "string",
          "enum": [
            "viewer",
            "native",
            "fold",
            "split",
            "gate-summary"
          ]
        },
        "source": {
          "type": "string"
        },
        "renderer": {
          "type": "string"
        },
        "verdict_map": {
          "type": "object"
        }
      }
    },
    "execution": {},
    "scan_scope": {}
  },
  "description": "Boundary contract for the sast-gate gate. Permissive stage boundary; required mirrors the YAML required_fields. Automatic machine gate: PASS completes; FAIL/ERROR retry once then the named-human queue; execution records replay vs runner provenance (replay = reference corpus, no scanner ran)."
}
