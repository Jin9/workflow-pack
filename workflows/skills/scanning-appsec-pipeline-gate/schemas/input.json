{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "scanning-appsec-pipeline-gate input",
  "type": "object",
  "required": ["target_env", "sbom", "baseline", "idempotency_key"],
  "additionalProperties": false,
  "properties": {
    "target_env": {
      "type": "string",
      "description": "Reachable running app URL or handle to attack with DAST in the sandbox."
    },
    "sbom": {
      "type": ["object", "null"],
      "description": "Software bill of materials / dependency manifest for SCA. Null skips dependency scanning."
    },
    "baseline": {
      "type": ["object", "null"],
      "description": "Prior accepted findings to gate against. Null on first run (no baseline)."
    },
    "severity_floor": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low"],
      "default": "high",
      "description": "New findings at or above this severity block the gate."
    },
    "tier": { "type": "string", "enum": ["T1", "T2", "T3"], "default": "T1" },
    "idempotency_key": { "type": "string" }
  }
}
