{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "handoff-to-deploy input",
  "type": "object",
  "required": ["verdict", "evidence", "idempotency_key"],
  "additionalProperties": false,
  "properties": {
    "verdict": { "type": "string", "enum": ["pass", "conditional", "fail"], "description": "QA sign-off verdict from qa-validate. Only pass (or an approved conditional) may hand off." },
    "evidence": { "type": "object", "description": "QA evidence backing the verdict (from executing-qa-test-suite)." },
    "release_ref": { "type": ["string", "null"], "description": "Immutable reference to the artifact/commit being released." },
    "approver": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "description": "Named human with release authority (may also be captured at the gate).",
      "properties": {
        "name": { "type": "string" },
        "role": { "type": "string" }
      }
    },
    "target_env": { "type": ["string", "null"], "description": "Live environment to promote to." },
    "tier": { "type": "string", "enum": ["T1", "T2", "T3"], "default": "T1" },
    "idempotency_key": { "type": "string" }
  }
}
