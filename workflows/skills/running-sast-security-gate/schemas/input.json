{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "running-sast-security-gate input",
  "type": "object",
  "required": ["source_ref", "baseline", "idempotency_key"],
  "additionalProperties": false,
  "properties": {
    "source_ref": {
      "type": ["object", "string"],
      "description": "Developer-authored source to scan: a repo handle/commit, or the set of changed files."
    },
    "baseline": {
      "type": ["object", "null"],
      "description": "Prior accepted SAST findings to gate against. Null on first run (no baseline)."
    },
    "severity_floor": {
      "type": "string",
      "enum": ["critical", "high", "medium", "low"],
      "default": "high",
      "description": "New findings at or above this severity block the merge."
    },
    "tier": { "type": "string", "enum": ["T1", "T2", "T3"], "default": "T1" },
    "idempotency_key": { "type": "string" }
  }
}
