{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://squad-delivery/skills/researching-ba-problem-space/schemas/input.json",
  "title": "researching-ba-problem-space input",
  "description": "ENFORCED \u2014 the engine validates this ASSEMBLED stage input against this schema BEFORE the stage runs (engine/validation.py validate_stage_input; fail-closed, engine/config/runtime-binding.yaml input_validation: enforce). Shape below is the POST-adapter payload delivered at stage s1-discovery.",
  "type": "object",
  "required": [
    "raw_request",
    "requester",
    "normalized_request",
    "idempotency_key"
  ],
  "additionalProperties": false,
  "properties": {
    "raw_request": {
      "type": "string",
      "description": "From workflow input: raw_request."
    },
    "requester": {
      "type": "string",
      "description": "From workflow input: requester."
    },
    "normalized_request": {
      "type": "string",
      "minLength": 1,
      "description": "Pick from `intake`: the PII-redacted normalized request STRING."
    },
    "idempotency_key": {
      "type": "string",
      "description": "engine-injected: the workflow's single idempotency key, injected into every stage payload."
    },
    "upstream_artifacts": {
      "type": "object",
      "description": "engine-injected: map of producer stage id to its artifact path relative to this stage's dir.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "loop_back_feedback": {
      "type": "object",
      "description": "engine-injected (conditional): reviewer findings threaded into a re-run after a loop_back."
    }
  }
}
