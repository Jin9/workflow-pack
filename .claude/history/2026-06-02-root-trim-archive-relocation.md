# 2026-06-02 — Root trim + archive relocation + full S0–S7 demo

**Prior (2026-06-02 — root trim + archive relocation + full S0–S7 demo).** Three changes on top of the 2026-06-01 relocation: **(1)** the `.archive/` directory was **moved out of the workspace and renamed** — retired subprojects now live in the **sibling** directory `/Users/admin/Desktop/project/research/archive/` (named `archive`, **no leading dot**, and **outside** `workflow-pack/`); it holds `agentic-delivery-pipeline/`, `business-analyse/`, `ecommerce_mvp_business_only.v3.md`, and several dated bootstrap/UX kits. **(2)** `design-diagram/` was **removed** as a subproject (its raw-XML house-style draw.io role is now served by the external **`drawio` skill**), and `repo-generator/` was **relocated into `reference/repo-generator/`**. **(3)** `tmp/runs/shoppilot/` now holds a **full S0–S7 walkthrough demo** (one folder per stage, each with a README + built artifacts), not just S1. **Live root dirs are now: `reference/`, `templates-review/`, `tmp/`, `workflows/`** (plus `CLAUDE.md`, `README.md`, and `squad-delivery-dashboard.standalone.html`). *(Per the 2026-06-02 skill-consolidation note above, `skill-packs/` no longer exists: the 14 used skills live in `workflows/skills/`, and the ~100 unused ones were quarantined then deleted.)* The 2026-06-01 changes still hold: the **live delivery pipeline lives under `workflows/`** (`workflows/delivery-pipeline.yaml` canonical + `workflows/squad-delivery.yaml` 15-stage mirror + `workflows/schemas/`); **S1 BA Research is a discovery→brief composite** (details in its section below); the Rust engine + `workflow-v1.schema.json` are archived, so pipeline validation is **static** (YAML well-formedness + `ajv` on the boundary schemas + a `depends_on` graph check), not a `squad-engine` run; and the top-level design docs were consolidated — `SQUAD_DELIVERY_WORKFLOW_DESIGN.md` + `harness-engineer-workflow.md` → `README.md`, and `squad-delivery-overview.html` → `squad-delivery-dashboard.standalone.html` (the offline visual core-plan). Sections below that still describe the archived `agentic-delivery-pipeline/reference/...` layout document the *design* and the archived tree; for live work use `workflows/`.
