# 2026-06-06 — Offline draw.io stage-flow diagram

**Prior (2026-06-06 — offline draw.io stage-flow diagram).** Added **`delivery-pipeline-flow.drawio`** at the workspace root — a single **offline** draw.io view of the whole pipeline rendered **top-to-bottom in 8 SDLC phase bands** (Requirements → Design gate → Contracts → Development with the **Backend ∥ Frontend legs side-by-side** → cross-cutting CI/security → QA·SIT/UAT → Release → Operate), all **27 nodes** (15 S-stages S0–S7 + 12 test/security gates T1–T12), each box showing its **input ▸ / output ◂**. Human review is **colour-graded by the `gate` field**: 🟪 *strong* (sync/human named approval — S2, S4c, S5, S6, S7, T10) · 🟨 *review* (async-confirm / red-team gate — S1a, S1b, S1.5, S2.5, S4a-r, S4b-r) · 🟦 *auto* (the four `auto+exc` gates tagged *human on exception*); **12 of 27** carry a 👤 marker. Deferred/unbuilt nodes (S0, S3, S5, S7) have a **dashed border**; loop-backs/compensation are **dashed-red** edges. It mirrors **`dashboard-data.json`** (same 27 nodes / gate classes) and is **offline** (no `https://`/`src=`/CDN); boxes are sized to their wrapped text and the central spine keeps a box-free corridor with per-edge exit/entry anchors, so **no characters and no arrows cross a box**. Raw-XML house style (never Mermaid — the external `drawio` skill); when `dashboard-data.json` changes, refresh the diagram to match.
