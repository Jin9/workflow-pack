# 2026-06-04 — Skill-version pinning + boundary-schema closure + spec-validator gate

**Prior (2026-06-04 — skill-version pinning + boundary-schema closure + spec-validator gate).** Three banking-grade hardening edits to the live pipeline + a new **opt-in** spec-validation gate. **(1) Version pinning:** every **built** `skill_ref` in `workflows/delivery-pipeline.yaml` is now **exact-pinned** — the `^1.0.0`/`^1.5.0`/`^0.1.0` caret ranges became `1.0.0`/`1.5.0`/`0.1.0`, and the 5 previously-unpinned built refs gained an exact `skill_version`; `scoping-ba-intake` gained a top-level `version: 1.0.0`. `handoff-revoke` stays unpinned (unbuilt, OI-002). One exact version + one skill folder = one immutable artifact. **(2) Schema closure:** root `additionalProperties: false` added to `workflows/schemas/{discovery,delivery-pipeline-input,delivery-pipeline-output}.json` (OpenAI strict-mode cross-vendor portability); the demo artifacts still validate, and `ba-brief.json` + the `Translations`/`convention_overrides` value-maps stay open by design. **(3) Spec-validator gate + tooling:** new root `spec-review/` folder = a `universal-spec-validator` run + the risk-tiered `.spec-validator.yaml` (C4/C3 downtiered to warn, C5 on schema `^`-patterns ignored) + before/after reports + `SPEC-REVIEW.md` (audit + rationale). Opt-in (copy `.spec-validator.yaml` to root + `--config`); offline, static, CI-wireable. C4 stays warn on built skills (false positive — the autonomous S3/S4a/S4b stages); re-escalate to block when `handoff-to-deploy`/`handoff-revoke` are authored. The **dashboard data + bundle** (`dashboard-data.json` → `squad-delivery-dashboard.standalone.html`, via `roundtripping-dashboard-data-contract`) and **`README.md`** were re-synced to the exact pins.
