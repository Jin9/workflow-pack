# 2026-06-04 — S1→S2.5 ShopPilot demo run + contract debug-viewer skill; repo now on GitHub

**Prior (2026-06-04 — S1→S2.5 ShopPilot demo run + contract debug-viewer skill; repo now on GitHub).** Ran the live pipeline's first steps end-to-end on the ShopPilot fixture and committed the result. **S1** was re-stamped to the pinned skills `researching-ba-problem-space 1.0.0` → `eliciting-banking-brief 1.5.0` (content reproduced — the skills are idempotent and **RB-01** suppressed the discovery handoff, so `ba-research` ran on `raw_content` alone). **S1.5** `generate-ux-pack 0.1.0` produced an honest BA-brief-only UX pack (`tmp/runs/shoppilot/S1.5-ux-intake/`, **maturity 1** — brand tokens + all Thai microcopy `TBD`, since no UX-team drop existed). **S2** `designing-tech-lead-handoff 0.1.0` produced the full TL handoff (`S2-tl-design/output.json` + `design/architecture/`: component_map · api_contracts · 8 ADRs · 8 L4 specs · split event-catalog). **S2.5** `red-teaming-implementation-plan 0.1.0` exercised the **cap-1 loop-back**: round-1 **REVISE** on a dual-write / partial-failure gap → **ADR-008** (transactional outbox + idempotent retry) → round-2 **PROCEED** (a same-model self-review caveat is recorded in `bias_checks`; a cross-model re-review is recommended before the L3 gate). The three boundary schemas `workflows/schemas/{ux-intake,tl-design,plan-review}.json` are **referenced by the YAML but still absent** — each stage was validated against its own skill `schemas/output.json` instead. New **workspace utility skill `.claude/skills/rendering-contract-debug-viewer/`** renders any pipeline JSON contract into one self-contained, **offline**, dashboard-themed HTML viewer (light+dark, contract-aware badges for `verdict`/`severity`/`tier`/etc., and a pretty-printed **View source** toggle) — sibling to `roundtripping-dashboard-data-contract`. The dashboard was re-synced (no semantic data change; bundle byte-identical).
