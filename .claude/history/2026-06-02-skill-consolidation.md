# 2026-06-02 — Skill consolidation (skill-packs split by use → workflows/skills/)

**Prior (2026-06-02 — skill consolidation).** The `skill-packs/` staging copy was **split by actual use**: the **14 skills the live pipeline references** were moved into **`workflows/skills/`** (so `skill_ref: skills/<name>`, which resolves relative to the workflow file's dir, now points at a real folder), and the **~100 unused skills** (all 8 packs, `design-time-orchestrators/`, most of `standalone/`, and 13 of `squad-delivery-skills/`) plus the `manifest.json` registry were first quarantined and then **DELETED** (`_unused-skills/` no longer exists). Deletion is **permanent** — there is no git here and the upstream `treasury/` is not present in this workspace, so those copies are gone (they remain re-derivable only from the external `treasury/` source). The stale S2 ref `tl-design-from-brief` was corrected to `designing-tech-lead-handoff` in both workflow YAMLs. The kept set + provenance is in `workflows/skills/README.md`. Two referenced skills remain unbuilt (`handoff-to-deploy`, `handoff-revoke`; OI-002). *(Superseded: the live set is now 28 skills; the deploy pair + 12 T-gates were since built, OI-002 closed.)*
