# 2026-06-02 — Dashboard S1 split into two cards

**Prior (2026-06-02 — dashboard S1 split into two cards).** `squad-delivery-dashboard.standalone.html` now **renders S1 as two stage cards** — `S1a · BA Discovery` (`researching-ba-problem-space`; the supervision gate on the `proceed`/`needs-work`/`do-not-build` recommendation) and `S1b · BA Brief` (`eliciting-banking-brief`; epics/stories) — instead of one collapsed `S1 · BA Research (composite)` card, so the human decision gate **between** the two YAML nodes is visible as its own boundary. The pipeline YAMLs were **already** two gated nodes (`s1-discovery` + `ba-research`); this was a **visual/data-model change only** (no YAML change). Edited inside the bundle's DATA module (`stages[]`, `EVENTS` `ba.discovery`→`ba.brief`, `TEMPLATES`, `skillmap[]`); RENDER untouched (it merges by `s.id`). README + this file refreshed to match.
