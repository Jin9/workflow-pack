# 2026-06-07 — S2 skill narrowed to the system-design blueprint (0.2.0)

**Prior (2026-06-07 — S2 skill narrowed to the system-design blueprint).** Re-scoped the S2 stage skill **`workflows/skills/designing-tech-lead-handoff`** (a skillify *Split/scope-narrowing* refactor, **0.1.0 → 0.2.0**, folder name kept) from the eight-concern "full TL handoff" down to the **system-design blueprint only**. **KEPT:** bounded contexts + layer-presence, `component_map`, `api_contracts` (the cross-component *interface* contracts), the 4 infra docs, ADRs, the consolidated offline L1–L4 architecture `.drawio` (+ its `.spec.json` + the deterministic `scripts/spec_to_drawio.py` generator), the consolidated ER/`data_model` boundaries, and the *architectural identification* of orchestrators (component + ADR). **TRIMMED + documented (not built — split target in `audit/RATIONALE.md §0`):** per-service API specs + the event catalog → S3 `contract-design`; per-story L4 specs + the detailed orchestrator state-machine spec → a future detailed-design skill. Removed `references/{orchestrator-scaffold,event-catalog-split,mermaid-conventions}.md`, `templates/{api-spec,l4-spec,orchestrator}.md`, `tests/assertions/event-split-discipline.md`; dropped `output.json` `l4_specs`/`event_catalog`/`api_specs`/`orchestrators` and `input.json` `mode`/`story_id`/`prior_design_ref` (the `spec` mode is gone). The permissive boundary `workflows/schemas/tl-design.json` (requires only `component_map`/`api_contracts`/`audit_id`) is **unchanged** and the downstream `from_stage` wiring (S2.5/S3/S4a/S4b consume only `component_map`/`api_contracts`) is **unaffected** — so the narrowing is contract-safe. **Demo refreshed** (`tmp/runs/shoppilot/S2-tl-design/`): `shoppilot-architecture.drawio` regenerated offline-clean by the generator (the stale demo carried **7 CDN icon URLs** — an offline violation; 687 KB → 135 KB pure-vector, 5 tabs, byte-deterministic), `output.json` trimmed to the narrowed contract, and the orphaned detailed-design files moved verbatim to `_deferred-detailed-design/`. **Ripple:** exact `skill_version` pin bumped to `0.2.0` in `workflows/delivery-pipeline.yaml`, `dashboard-data.json` (+ bundle rebuilt via `roundtripping-dashboard-data-contract`, `--verify` in-sync), and `delivery-pipeline-flow.drawio` (its `◂ out:` was already `component_map · api_contracts`). **Verified:** skill `quick_validate`+`check_links` exit 0; demo `output.json` validates against both the narrowed `schemas/output.json` and the boundary `tl-design.json`; the diagram is offline-clean + byte-identical on rebuild. *(Not regenerated: the pre-built `tmp/runs/shoppilot/delivery-review.html` console snapshot — its S2 path-chips/SVG now point at the moved files; refresh via `rendering-delivery-review-console` is a deferred follow-up.)*
