# 2026-06-06 — Human-review output-contract design + the Delivery Review Console

**Prior (2026-06-06 — human-review output-contract design + the Delivery Review Console).** Designed and built the **human-review document scheme** for the dual-output pipeline (recorded in **`reference/plan/output-contract-and-review-design.md`** — the dual-output principle, a per-node *remediation matrix* mapping every raw-JSON contract to a human-view disposition `FOLD | DEDICATED | NATIVE | SPLIT | leaf-gate-summary`, the menu taxonomy, and the gates/owners table). The centrepiece is a new tooling skill **`.claude/skills/rendering-delivery-review-console/`** that assembles **one run-scoped, offline, byte-deterministic `delivery-review.html`** per run folder — a *single standalone document* whose **left-nav menus are pipeline stages** (menu1 Epics&Stories · menu2 UX Brief · menu3 Design&ADRs · menu4 Plan Review · menu7 Quality …), **superseding the per-stage viewers** for review (`rendering-contract-debug-viewer` stays for one-off single-file debug). It generalises `eliciting-banking-brief`'s `ba-research-viewer.template.html` app-shell into a data-driven `window.PACK.menus[]`/`sections[]` renderer and ports the contract-aware badges from `rendering-contract-debug-viewer`. **Diagrams embed as offline inline SVG** via a deterministic `scripts/drawio_to_svg.py` transcoder (reads `.drawio` mxGeometry → `<svg>`, **not** a raster/PNG export — that would break the offline + byte-determinism invariants) plus a path chip to the editable `.drawio`. The **12 T-gates roll into a Quality-Gate Board** (worst-of R/A/G; a per-gate `verdict_map` encodes all three vocabularies — T10 `pass|conditional|fail`, T12 `promote|hold|rollback`, the rest `PASS|FAIL|ERROR` — without flattening them). Closed the **3 previously-missing boundary schemas** `workflows/schemas/{ux-intake,tl-design,plan-review}.json` (the YAML `schema_ref`s now resolve) and added the **additive, optional `human_view`** render-policy field to `discovery.json` + `ba-brief.json` + the 12 T-gate schemas (in `properties`, never `required`, so all existing artifacts still validate); `ux-intake.json`'s `maturity_level` is an **integer 0–3** mirroring the built `generate-ux-pack`, and `tl-design.json` carries **optional** `data_model`/`diagrams` refs (the per-service *ER-pack* split is **deferred** — recorded as target state). Demoed on **`tmp/runs/shoppilot/delivery-review.html`** (S1a–S2.5 render richly incl. the 2 transcoded diagram SVGs; S0/S3–S7 show honest *pending* stubs; the gate board shows the *no-evidence* empty state) and the board proven on a synthetic `tmp/runs/_fixtures/gateboard-demo/` (RED rollup). Independently verified: skill `quick_validate`+`check_links` exit 0, console **offline-clean** (zero `http(s)`/`src=`/`@import`/`@font-face`/`url(`), **byte-deterministic** on rebuild, both inline `<script>` blocks pass `node --check`, all 21 boundary schemas valid draft-07 and the 3 demo artifacts validate. Still **deferred**: mirroring `human_view` into `dashboard-data.json`'s registry + bundle rebuild (optional, no console impact).
