# 2026-06-06 — `templates-review/` deletion (aside)

**`templates-review/` deletion (2026-06-06).** `templates-review/` was a **non-destructive review copy** of the (now-archived) `../archive/agentic-delivery-pipeline/reference/integration/templates/` tree. It was **deleted on 2026-06-06** (timestamped Trash move; git-tracked, so `git checkout` restores it) — the canonical templates live in the archived tree. If a review copy is needed again, re-derive it from there (off-limits without permission).
