# 2026-06-07 — Root cleanup: retired the stage-flow `.drawio` (later reverted)

**Latest (2026-06-07 — root cleanup: retired the stage-flow `.drawio`).** Removed the root **`delivery-pipeline-flow.drawio`** (`git rm` — recoverable from git history) to keep the workspace root clean. The **offline dashboard** (`squad-delivery-dashboard.standalone.html` + its source-of-truth `dashboard-data.json`) remains the **canonical visual core-plan** for the whole pipeline (same 27 nodes / gate classes the diagram mirrored), and the editable per-run architecture diagram still lives under `tmp/runs/` (`tmp/runs/shoppilot/S2-tl-design/diagrams/shoppilot-architecture.drawio`). `README.md` and the convention bullet below were updated to drop the now-dead root-file pointer; the older dated *"Prior"* notes that mention the diagram (and the two historical mentions in `workflows/skills/designing-tech-lead-handoff/audit/RATIONALE.md`) are **left intact as honest history** — they record past state, not a live instruction. No other artifact referenced the file (the dashboard bundle and `dashboard-data.json` did not). *(NOTE: this retirement was later **reverted** — `delivery-pipeline-flow.drawio` is present again at the root as of commit `1944057 revert this file`. See the current `CLAUDE.md` for live state.)*
