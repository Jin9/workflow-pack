# 2026-06-06 — Spec-review reports refreshed + `templates-review/` deleted

**Prior (2026-06-06 — spec-review reports refreshed + `templates-review/` deleted).** Re-ran the optional `universal-spec-validator` gate over the **full post-build spec set** and refreshed the four `spec-review/` report artifacts (the prior reports predated the S6 + T1–T12 build and counted only **47 specs**). Current run: **104 specs** (28 `SKILL.md` + 54 skill schemas + 18 boundary schemas + 1 YAML + 3 `.claude/`); **raw** = 50 block / 61 warn / exit 1, **tuned** (the in-folder `.spec-validator.yaml`) = **0 block / 68 warn / exit 0** (green). `SPEC-REVIEW.md`'s verdict + Run-table + per-rule triage were synced; notably the once-`1 REAL` `delivery-pipeline.yaml` C5 is **gone** (exact pins held — all 15 remaining C5 are schema-file `^pattern` false positives), and the C4 warn list grew **11→18** as the 7 new T-gate runners trip the `prod`-substring heuristic (all false positives; the deploy pair carries `requires_approval: true` and does not trip C4). Separately, **`templates-review/` was deleted** (timestamped Trash move, git-restorable — it was a deletable review copy of the archived templates tree). Per this file's "newest dated note supersedes older prose" convention (cf. the line above on the git-repo note), the authoritative live state is now **28 skills / 27 stages / OI-002 closed** (the `handoff-to-deploy` · `handoff-revoke` deploy pair + the 12 T1–T12 test gates are built and wired); older "14 skills / OI-002 open" prose below is historical.
