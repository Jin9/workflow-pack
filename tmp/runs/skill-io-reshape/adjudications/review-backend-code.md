# Adjudication — review-backend-code (S4a backend-review)

Source: codex/review-backend-code.md · exit=0. Verified: input.json models design_document/
implement_stage_output/severity_floor vs the real picks (files_generated + tests_generated from
backend-implement, api_contracts from tl-design — tl-design picked transitively, loader allows ancestor
picks); corpus backend-review.json has NO audit_id, string claims arrays, audit_metadata
{rules_evaluated: 22, files_scanned: 45, lines_scanned: 3463} where the manifest holds 45 production +
24 test files (files_scanned counted production only — Codex's split confirmed against the manifest);
YAML failure_policy loops ONLY to backend-implement (max 2 → human-queue) — a "design" loop target is
unexecutable; rules_evaluated 22 matches the rubric count.

| F# | Sev | Verdict | Rationale / modified form |
|----|-----|---------|---------------------------|
| F1 | blocker | **ACCEPT** | input.json ← stub (files_generated/tests_generated/api_contracts + injected; ADVISORY marker); phantom design_document/target_package/implement_stage_output/code_under_review/tests_under_review/convention_overrides retired; harness fixture updated. 1.0.0 → 2.0.0 + pin. |
| F2 | blocker | **ACCEPT** | Review transform made explicit: resolve upstream_artifacts["backend-implement"] → producer artifact = claim truth; producer dir = root; manifest paths resolved safely BENEATH root; content_hash verified; THEN read source. Missing path / traversal / unreadable / hash mismatch ⇒ human-queue input_incomplete, never an inferred verdict. |
| F3 | blocker | **ACCEPT** | audit_id required in skill+boundary+YAML; house formula UUIDv5(HOUSE_NS,"backend-review:{idempotency_key}") documented for LIVE; corpus gains audit_id via simulate.py aid("S4a-backend-review") — the grandfathered corpus-generator convention (same pattern as the s4a/s4b/s4c chunks; ADDING a missing field via the generator ≠ provenance rewrite). |
| F4 | blocker | **ACCEPT** | Verdict matrix schema-enforced: approve ⇒ null target ∧ no P1/P2 ∧ all claim checks verified/not-applicable; loop_back ⇒ no P1 ∧ (P2 ∨ unverified claim) ∧ target backend-implement; human-queue ⇒ P1 ∧ null target. Design ambiguity → human-queue (YAML cannot loop to tl-design — verified). target documented as consistency record; YAML authoritative. |
| F5 | major | **ACCEPT** | Workflow idempotency_key (review identity, derives audit_id) vs application idempotency (reviewed from api_contracts.contracts[].idempotency_rules + producer runtime_idempotency/idempotency_strategy) disentangled; UUID-v4 application-key assumption removed from step 6 + rubric A2. |
| F6 | major | **ACCEPT** | severity_floor REMOVED — a suppression vector (P1 floor hides P2s while the engine forwards only emitted findings to regeneration = blind remediation loop). Canonical payload never supplied it. Every P1/P2/P3 always emitted; filtering is presentation-only. |
| F7 | major | **ACCEPT-MODIFIED** | claims_verified/claims_unverified → required claim_checks[] with the HOUSE shape shared with frontend-review: {claim_ref, status: verified|unverified|not-applicable, evidence} + optional claim_type/location; unverified ⇒ finding_ref required (actionable loop-back). Corpus migrated via simulate.py (same semantic content, typed — not fabrication). |
| F8 | major | **ACCEPT** | rules_evaluated const 22 (rubric-verified); files_scanned → production_files_scanned + test_files_scanned (≥1 each) + positive lines_scanned; corpus values DERIVED by simulate.py from the manifest (45 prod + 24 test confirmed; lines summed from the manifest's real per-file data, not Codex's arithmetic). |
| F9 | major | **ACCEPT** | confidence required per finding; dead `default` annotation removed; Low-confidence ⇒ evidence carries the needs-verification marker; severity documented as post-confidence-adjustment (feeds the verdict matrix). |
| F10 | minor | **ACCEPT** | ADVISORY marker replaces the false pre-invocation-validation claim; fenced validating examples; dead refs fixed (validate-build → qa-plan; adversarial → validating-banking-implementation; scans → SAST/AppSec skills; reviewing-software-security/chaos-plan removed). |

Version: 1.0.0 → 2.0.0 + YAML pin. Boundary required += audit_id == YAML (R3). Corpus
backend-review.json regenerated by simulate.py (audit_id + claim_checks + split scan metadata).
