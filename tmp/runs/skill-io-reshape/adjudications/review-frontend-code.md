# Adjudication — review-frontend-code (S4b frontend-review)

Source: codex/review-frontend-code.md · exit=0. Verified: input.json wraps an obsolete envelope vs the
real picks (files_generated/tests_generated from frontend-implement, api_contracts from tl-design);
corpus frontend-review.json has NO audit_id, string claims arrays, audit_metadata {rules 26, files 37,
lines 2316, claims_checked 4} where the manifest holds 37 prod + 3 test files; corpus does NOT carry
review_duration_inferred_seconds (schema removal corpus-safe; the tests/ fixture does carry it — fixture
patch); frontend-artifacts.json boundary is looser than the producer (items {path, content_hash,
size_bytes, lines_added}); YAML loops ONLY to frontend-implement; ux-intake is a transitive ancestor of
frontend-review (loader allows ancestor picks — the tl-design pick already relies on this).

| F# | Sev | Verdict | Rationale / modified form |
|----|-----|---------|---------------------------|
| F1 | blocker | **ACCEPT** | input.json ← stub + the new ux picks (below); ADVISORY marker; obsolete wrapper retired; tests/cases/001 input fixture rewritten to the assembled shape. 1.0.1 → 2.0.0 + pin. |
| F2 | blocker | **ACCEPT-MODIFIED** | YAML pick extension: frontend-review gains ux-intake picks — MODIFIED to the ux required set that review actually consumes (pack_dir, design_tokens_path, component_inventory_path, screen_states_path, form_validation_path, responsive_spec_path, accessibility_spec_path — all guaranteed by ux-intake required_fields since the ux chunk). Resolution procedure: upstream_artifacts["frontend-implement"] → producer artifact = claim truth; read dirname(artifact)/frontend_worktree_root/<manifest path> with hash verification; missing/mismatch ⇒ human-queue input_incomplete, never inferred approval. |
| F3 | blocker | **ACCEPT** | audit_id required in skill+boundary+YAML; house formula UUIDv5(HOUSE_NS,"frontend-review:{idempotency_key}") for LIVE; corpus gains audit_id via simulate.py aid("S4b-frontend-review") (generator convention — same as backend). Boundary desc rewrite. |
| F4 | major | **ACCEPT** | loop_back_target_stage enum = frontend-implement|null (the only executable edge — YAML verified); design_ambiguity/bundle_overrun/token_gap → human-queue w/ null target; severity guide + harness assertions updated. |
| F5 | major | **ACCEPT** | Verdict-matrix conditionals (house shape shared with backend): P1 ⇒ human-queue; unverified claim ⇒ never approve; approve ⇒ null target ∧ no P1/P2 ∧ complete audit counts; loop_back ⇒ executable target. confidence required per finding. Invariants move from the non-runtime harness into the schema. |
| F6 | major | **ACCEPT-MODIFIED** | claims arrays → claim_checks[] in the SAME house shape as backend-review ({claim_ref, status, evidence} + optional claim_type/location; unverified ⇒ finding_ref) — NOT a frontend-specific dialect; claims_checked derived == len(claim_checks) (schema-documented, sim-derived). Corpus + fixture migrated via simulate.py. |
| F7 | major | **ACCEPT** | frontend-artifacts.json boundary items mirror the producer: require path/content_hash/size_bytes, declare size_bytes, lines_added optional, component_pillar optional. Corpus validates (manifest already carries all three). |
| F8 | minor | **ACCEPT** | focus_management_evident / csrf_protections_verified become nullable (null = not applicable); the "report true when N/A" instruction removed — a display-only component no longer fabricates a verified-CSRF claim. |
| F9 | minor | **ACCEPT** | review_duration_inferred_seconds removed from schema/prose/fixture (nondeterministic; corpus never carried it — verified). |
| F10 | minor | **ACCEPT** | planning-banking-tests input.json: both nested review objects constrain verdict to approve|loop_back|human-queue + AP:false (matches the NESTED_HANDOFF payload exactly). |

Version: 1.0.1 → 2.0.0 + YAML pin. Boundary required += audit_id == YAML (R3). Corpus
frontend-review.json regenerated by simulate.py; tests/cases/001 fixtures re-rendered.
